<div class="container">
	<div class="form-group">
		<%= form_for @edit_gossip do |f| %>
		<label>Titre : </label>
		<%= f.text_field :title, class: 'form-control' %>
		<label>Contenu : </label>
		<%= f.text_field :content, class: 'form-control' %>
		<div class="form-group text-center">
			<input type="submit" name="Modifier" class="btn btn-success">
		</div>
		<% end %>
	</div>
</div>