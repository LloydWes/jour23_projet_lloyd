<% i = 1 %>
<!-- <div class="card-columns"> -->
	<!-- <div class="container"> -->
	<!-- <div class="card-deck"> -->
		<div class="flex-parent d-flex flex-wrap justify-content-around">
		<% Gossip.all.each do |current_gossip| %>
		<div class="card" style="width: 18rem;">
			<div class="card-header">Gossip n°<%=current_gossip.id%></div>
			<img class="card-img-top img_fl" src="gossip_img/<%= rand(1..3)%>" alt="Card image cap">
			<div class="card-body">
				<h5 class="card-title text-muted">De : <a href="<%= user_path(current_gossip.user.id) %>"><%=current_gossip.user.first_name%> <%=current_gossip.user.last_name%></a></h5>
				<p class="card-text"><%=current_gossip.content%></p>
				<div class="text-center">
					<a href="<%= gossip_path(current_gossip.id) %>" class="btn btn-primary">Voir le gossip</a>
				</div>
			</div>
			<div class="card-footer text-muted">Last update <%=current_gossip.updated_at.to_s.split(" ")[0...-1].join(" ") %></div>
		</div>
		<% if i==5 %>
		<% i = 0 %>
	<!-- </div> -->
	<!-- <div class="card-deck"> -->
		<% end %>
		<% i += 1 %>
		<% end %>
	<!-- </div> -->
</div>